# npm configuration for Vercel builds
# These settings help avoid cache conflicts during parallel builds
prefer-offline=false
audit=false
fund=false
progress=false
# Use npm's default cache location (Vercel manages it)
# Setting a custom cache location can cause conflicts on Vercel
