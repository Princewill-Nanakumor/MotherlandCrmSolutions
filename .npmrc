# npm configuration for CI builds
# These settings help avoid cache conflicts during parallel builds
prefer-offline=false
audit=false
fund=false
progress=false
# Use npm's default cache location (the CI provider manages it)
# Setting a custom cache location can cause conflicts in CI
