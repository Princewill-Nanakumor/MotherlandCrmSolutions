[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"
  NODE_ENV = "production"

# Disable route analysis during build to prevent MongoDB connection errors
[functions]
  node_bundler = "esbuild"
