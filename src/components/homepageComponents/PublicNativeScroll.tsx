// src/components/homepageComponents/PublicNativeScroll.tsx
"use client";

/**
 * @deprecated Scroll mode is owned by `UiZoomApplier` via `syncAppScrollMode`.
 * Kept as a no-op so existing homepage imports don't break.
 */
export function PublicNativeScroll() {
  return null;
}
